import { ReactComponent as ImagePlaceholderIcon } from '../../assets/icon/image-placeholder.svg';

import { ImagePlaceholderContainer } from './image-placeholder.style';

const ImagePlaceholder = () => {
	return (
		<ImagePlaceholderContainer>
			<ImagePlaceholderIcon />
		</ImagePlaceholderContainer>
	);
};

export default ImagePlaceholder;
